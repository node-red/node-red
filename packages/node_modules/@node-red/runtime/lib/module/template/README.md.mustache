{{&module}}
=====================

{{&desc}}

Install
-------

Upload {{&module}}-{{version}}.tgz from `Manage palette > Install` menu of Node-RED editor, or run the following command in your Node-RED user directory - typically `~/.node-red`

Run the following command in your Node-RED user directory - typically `~/.node-red`

        npm install {{&module}}-{{version}}.tgz

